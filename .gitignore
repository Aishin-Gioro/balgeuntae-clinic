# dependencies
/node_modules

# next.js
/.next
/out

# production
/build

# misc
.DS_Store
*.pem

# debug
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# env
.env*.local
.env

# 클라이언트 내부 문서 (저장소 공개 — 원격 미포함, 로컬 에이전트 참고용)
/docs

# typescript
*.tsbuildinfo
next-env.d.ts

# 이미지 원본(최적화 전) — 로컬 보관, 배포/커밋 제외
/image-src

# 관리자 편집기 로컬 저장소 (배포 환경은 Vercel Blob 사용)
/.data
/public/uploads

# 클라이언트 참고 자료(전자책 등) — 로컬 보관, 커밋 제외
/*.pdf

.vercel
